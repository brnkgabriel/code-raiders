#1 - As a general rule, you should use useTask$ for anything that's suitable for useTask$. Majority of the time you should use useTask$. If you need something to be rendered not on the server side but on the browser, use useVisibleTask$

continue from #4